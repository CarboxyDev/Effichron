- For api errors, I'm using (error as AxiosError).response.data for the error message when I should
probably be using error.message
- Clicking on the button in CTA section redirects to timer page which is fine but it doesnt scroll to the top
- Magic text doesn't wrap which is an issue on mobile