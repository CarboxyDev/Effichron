import { PageWrapper } from '@/components/PageWrapper';

export default function SignInPage() {
  return (
    <PageWrapper navbarProps={{ variant: 'with-branding', drawDivider: false }}>
      <div></div>
    </PageWrapper>
  );
}
